pipeline {
    agent any

    stages {
        stage('init') {
            steps {
                echo 'Pullling code'
            }
        }
        stage('build') {
            steps {
                echo 'building image'
            }
        }
        stage('deploying') {
            steps {
                echo 'pushing latest code'
            }
        }
    }
}
